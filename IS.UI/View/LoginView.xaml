<UserControl x:Class="IS.UI.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IS.UI.View"
             xmlns:Controls="clr-namespace:IS.UI.Control"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel LastChildFill="False">
            <Controls:IconButton 
                DockPanel.Dock="Right"
                Icon="Unlock"
                VerticalAlignment="Stretch"
                Style="{StaticResource IconButtonStyle}" Content="Login" />

        <StackPanel DockPanel.Dock="Right" Margin="10 0 0 10">
            <TextBlock Style="{StaticResource DefaultLabelTextBlockStyle}" Text="Password"/>
            <TextBox Text=""/>
        </StackPanel>
        <StackPanel DockPanel.Dock="Right" Margin="10 0 0 10">
            <TextBlock Style="{StaticResource DefaultLabelTextBlockStyle}" Text="Username"/>
            <TextBox Text=""/>
        </StackPanel>
    </DockPanel>
</UserControl>
